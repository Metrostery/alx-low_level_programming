#include <stdio.h>
/**
 * main - prints alphabets
 * using main
 *
 * Return: 0
 */

void print_alphabet(void);

int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
	{
		void print_alphabet(void);
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
